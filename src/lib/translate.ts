export async function translateText(_text: string, _lang: string): Promise<string> {
  return _text;
}

export async function translateProduct(product: any, _lang: string): Promise<any> {
  return product;
}

export async function localizeProduct(product: any, _lang: string): Promise<any> {
  return product;
}

export async function translateVendor(vendor: any, _lang: string): Promise<any> {
  return vendor;
}
